const circle = require('./circle.js');

const radius = 5;

console.log(`The area of a circle with radius ${radius} is: ${circle.area(radius)}`);
console.log(`The circumference of a circle with radius ${radius} is: ${circle.circumference(radius)}`);

